@model IEnumerable<PsychotherapistWebSite.Core.Models.Image>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/User/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Ekle", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

    @foreach (var image in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => image.Name)
            </td>
            <td>
                @Html.ActionLink("Detay", "Details", new { id=image.Id }) |
                @Html.ActionLink("Sil", "Delete", new { id=image.Id })
            </td>
        </tr>
    }

</table>

<div>
    @Html.ActionLink("Geri", "Index","Slide")
</div>
